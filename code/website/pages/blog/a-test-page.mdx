---
title: A Test Page using mdx, NextJS, SSG, Cloudflare
description: Blog, Yuzukicat, First Page, mdx, NextJS, SSG, Blog, Cloudflare
date: 2021-01-14
authors: yuzukicat
tags: [test]
---

# A Test Page

> 一些暴論

```rust showLineNumbers copy
#![feature(drain_filter)]
use std::collections::LinkedList;

let mut numbers: LinkedList<u32> = LinkedList::new();
numbers.extend(&[1, 2, 3, 4, 5, 6, 8, 9, 11, 13, 14, 15]);

let evens = numbers.drain_filter(|x| *x % 2 == 0).collect::<LinkedList<_>>();
let odds = numbers;

assert_eq!(evens.into_iter().collect::<Vec<_>>(), vec![2, 4, 6, 8, 14]);
assert_eq!(odds.into_iter().collect::<Vec<_>>(), vec![1, 3, 5, 9, 11, 13, 15]);
```

Reference [doc.rust-lang.org](https://doc.rust-lang.org/stable/std/collections/struct.LinkedList.html#method.remove)

