---
title: GraphQL Tools
description:
  Open Source developers with experience of working with the largest companies and applications.
  GraphQL consulting, workshops and trainings.
---

import { useSSG } from 'nextra/ssg'
import { ClientLogosSection } from '@/client-logos-section'
import { Button, Description, Heading, Newsletter } from '@/components'
import { HeroSection } from '@/hero-section'
import { getAllArticles } from '../lib/get-all-articles'
export const getStaticProps = async () => ({
    props: {
      ssg: await getAllArticles()
     }
})
export default function IndexPage() {
  return (
    <>
      <HeroSection>
        <Heading>Modern API Platform and ecosystem that scales</Heading>
        <Description>
          The Guild's advanced open source ecosystem covers everything you need for your API
          infrastructure with a modular, open source and complete platform
        </Description>
      </HeroSection>
    </>
  )
}